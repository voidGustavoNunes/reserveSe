import 'package:flutter/material.dart';

class BanheiroWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 50,
      height: 50,
      color: Colors.blue,
      child: Center(child: Text("Banheiro")),
    );
  }
}