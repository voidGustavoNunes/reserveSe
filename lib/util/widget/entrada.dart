import 'package:flutter/material.dart';

class EntradaWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 50,
      height: 50,
      color: Colors.black12,
      child: Center(child: Text("Entrada")),
    );
  }
}