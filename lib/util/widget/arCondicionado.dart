import 'package:flutter/material.dart';

class ArCondicionadoWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 50,
      height: 50,
      color: Colors.amber,
      child: Center(child: Text("Ar Condicionado")),
    );
  }
}